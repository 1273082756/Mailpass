# 邮件接收域名列表，使用逗号分隔。DNS 的 MX 记录需要指向这台机器。
MAIL_DOMAINS=mail.example.com,example.com

# Web 界面端口与 SMTP 入站端口。
WEB_PORT=8080
SMTP_PORT=25

# 可选配置。
SMTP_ENABLED=true
MAX_MESSAGE_SIZE=15728640
CORS_ORIGINS=*

# 可选：留空时后端会生成随机密钥，并通过 `docker compose logs backend` 输出。
# ACCESS_KEY=
