[project]
name = "mailpass-backend"
version = "0.1.0"
description = "Receive-only temporary email backend"
requires-python = ">=3.11,<3.12"
dependencies = [
  "aiosmtpd==1.4.6",
  "fastapi==0.115.6",
  "uvicorn[standard]==0.34.0",
]

[tool.uv]
package = false
